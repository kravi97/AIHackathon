@page "/"
@using INTIME.Maui.Services.Navigation
@inherits INTIMEMainLayoutPageComponentBase

@code {
    protected override void OnInitialized()
    {
        Resolve<INavigationService>().NavigateTo(NavigationUrlConsts.User);
    }
}